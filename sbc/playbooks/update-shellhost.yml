---
- name: update shellhost on penduino experiment
  hosts: penduino
  become: yes
  gather_facts: yes
  
  tasks:

  - name: Include update-shellhost-rpios task
    include: ../tasks/update-shellhost-rpios.yml

  handlers:

  - name: Include shellhost handlers
    include: ../tasks/shellhost-handlers.yml
